{
  "$id": "https://schema.legytma.com.br/1.1.2/schema/bool.schema.json",
  "$schema": "http://json-schema.org/draft-06/schema#",
  "$comment": "Used to identify parser. Every parser can permit only one type",
  "title": "Boolean",
  "description": "Define the representation of boolean acceptable.",
  "type": "boolean",
  "examples": [true, false]
}
